---
tags:
  - ag-core
  - docs
  - guide
  - conventions
---

# ag-core 文档组织规范

> 本文档定义了 ag-core 文档的目录结构、命名约定、写作规范和维护原则。

## 目录结构

文档根目录：`/opt/hermes_vault/ag-core-docs/docs/`

### 分类映射规则

| 源码路径 | 文档分类目录 | 说明 |
|---------|-------------|------|
| `ag/` | `核心框架/` | 框架核心模块 |
| `contribute/` | `集成适配/` | 第三方集成/适配层 |
| `tool/cmd/` | `CLI工具/` | 命令行工具 |
| 代码生成流程 | `代码生成/` | 端到端代码生成 |
| 架构决策 | `架构决策/` | ADR、优化方案 |
| 项目总览 | `docs/` 根目录 | 项目概览、开发指南 |

### 顶层文件

```
ag-core-docs/
├── 文档组织规范.md     ← 本文档（元规范）
├── 人工补充材料/       ← 非正式补充文档
├── 额外材料/           ← 过渡期辅助文档
└── docs/               ← 正式文档（发布用）
    ├── index.md               ← 总索引（MOC），所有模块的入口
    ├── 01-项目概览.md       ← 项目定位、技术栈、目录结构
    ├── 核心框架/            ← ag/ 目录映射
    ├── 集成适配/            ← contribute/ 目录映射
    ├── CLI工具/             ← tool/cmd/ 目录映射
    ├── 代码生成/            ← 代码生成流程
    ├── 架构决策/            ← ADR 和优化方案
    └── 开发指南/            ← 开发环境搭建、扩展示例
```

## 模块文档结构

每个模块遵循统一模板：

```
模块名/
├── 00-Ag模块名.md           ← 入口文件
├── 01-子主题A.md            ← 架构深化
├── 02-子主题B.md
├── ...
├── MM-使用指南.md            ← 最后一篇，面向业务开发者
└── 复杂子包名/              ← 复杂子包独立子目录
    ├── 00-总览与分析.md
    ├── 01-xxx.md
    └── ...
```

### 入口文件 `00-Ag模块名.md`

必须包含以下章节：

1. **概述** — 一句话定位模块
2. **架构分层** — 层次结构（可用表格或流程图）
3. **文件结构** — 源码目录树 + 每个文件一句话说明
4. **文档目录表** — 子文档的链接 + 内容说明
5. **关键设计** — 该模块的核心设计模式/概念（表格形式）

### 架构文档 `01~N-1`

面向**框架开发者和维护者**，内容包含：

- 类层次结构（接口、实现类、继承关系）
- 数据流/执行流程
- 关键函数的源码分析（含文件路径 + 行号）
- 设计模式说明
- 调用方/被调用方关系

格式以表格、代码块（含行号范围注释）、流程图为主。

### 使用指南 `NN-使用指南.md`

面向**业务开发者**，要求：

- **不提框架内部概念**（如：不出现 FX Module 内部细节、不出现抽象类名如 `AbstractPropertyResolver`）
- 纯实用视角：快速开始 → 配置 → 示例代码 → 最佳实践 → FAQ
- 所有代码示例基于**实际项目中的常见用法**
- 每个示例自包含、可复制使用

## 写作规范

### 命名约定

| 条目 | 规则 | 反例 |
|------|------|------|
| 入口文件 | `00-Ag模块名.md` | ~~`00-索引.md`~~ |
| 架构文档 | `01-主题.md` 或 `02-主题.md` | 无编号 |
| 使用指南 | `NN-使用指南.md` | 非最后一位 |
| 子包目录 | 子包名（如 `agslog/`） | 无子目录 |
| 子包入口 | `00-总览与分析.md` | 无 |

> **入口文件命名说明：** 用 `00-AgLog.md` 或 `00-AgConf-概览.md` 这类描述性名称而非 `00-索引.md`，以避免 Obsidian 图谱视图将所有节点显示为 "00-索引"。

### 格式要求

- **YAML frontmatter**：每篇文档必须有 `tags` 元数据
- **WikiLink**：使用 `[[链接]]` 进行交叉引用，不写死相对路径
- **表格**：结构化信息优先用表格呈现
- **代码块**：标注语言（go/yaml/json/protobuf），关键行加注释说明
- **行号引用**：源码分析时标注文件路径+行号，如 `// zfx_agdb.go:10-27`

### 写作原则

1. **源码驱动**：所有技术内容基于项目实际源码逐行阅读后编写，不杜撰
2. **内容边界**：一篇文章只讲一个模块/子包，不混入无关模块
3. **分层读者**：架构文档和技术文档分离，使用指南不出现框架内部概念
4. **准确性优先**：代码示例必须匹配实际代码库的模式，不发明伪代码
5. **一致性**：同一套排版风格贯穿所有模块文档

## 已完成的模块

| 模块 | 文档目录 | 状态 |
|------|---------|------|
| ag-conf 配置管理 | `核心框架/ag-conf/` | ✅ 10 篇 |
| ag-log 日志框架 | `核心框架/ag-log/` + `agslog/` | ✅ 6+6 篇 |
| ag-app 应用启动 | `核心框架/ag-app/` | ✅ 4 篇 |
| ag-server 服务抽象 | `核心框架/ag-server/` | ✅ 1 篇 |
| ag-service 服务注册 | `核心框架/ag-service/` | ✅ 1 篇 |
| ag-error 错误处理 | `核心框架/ag-error/` | ✅ 1 篇 |
| ag-crypto 加密 | `核心框架/ag-crypto/` | ✅ 1 篇 |
| ag-common 工具 | `核心框架/ag-common/` | ✅ 2 篇 |
| ag-ext 扩展 | `核心框架/ag-ext/` | ✅ 4 篇 |
| agdb GORM 数据库 | `集成适配/agdb/` + `agdao/` | ✅ 6+1 篇 |

## 新增模块步骤

1. 在 `index.md` 的对应分类下添加链接
2. 创建模块入口 `00-Ag模块名.md`
3. 按主题拆分为 01、02...架构深化文档
4. 最后编写 `NN-使用指南.md`
5. 复杂子包拆分子目录
